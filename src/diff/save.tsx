/**
 * Save — returns null since diff blocks are transient editor-only wrappers.
 * Content is managed server-side via the DM content abilities.
 */
export default function Save(): null {
	return null;
}
